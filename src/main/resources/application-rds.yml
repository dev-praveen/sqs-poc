spring:
  config:
    import: aws-secretsmanager:sqs-creds
  application:
    name: sqs-poc
  cloud:
    aws:
      credentials:
        access-key: ${access_key}
        secret-key: ${secret_key}
      region:
        static: us-east-1
  datasource:
    url: jdbc-secretsmanager:postgresql://rds-postgres.czmuci46ietm.us-east-1.rds.amazonaws.com:5432/postgres
    username: rds!db-95505ded-d1cc-43f6-acd0-efc7c7c5fa85
    driver-class-name: com.amazonaws.secretsmanager.sql.AWSSecretsManagerPostgreSQLDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
sqs:
  queue:
    url: ${ecomm_queue_url}